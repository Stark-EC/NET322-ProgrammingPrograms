// Client
import java.io.*;
import java.net.*;

public class Client {
    public static void main(String[] args) {
        try {
            Socket clientSocket = new Socket("localhost", 12345); // Connect to server
            System.out.println("Connected to server.");

            // Handle client communication (e.g., read/write data)
            // You can create separate input/output streams for communication
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}